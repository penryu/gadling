export enum Emoji {
  // emoji to display when a procedure succeeds
  OK = 'heavy_check_mark',
  // emoji to display when a procedure fails
  FAIL = 'poop',
  RIGHT = 'thumbsup',
  WRONG = 'thumbsdown',
}
